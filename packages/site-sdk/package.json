{
  "name": "@auth4agents/site-sdk",
  "version": "0.1.0",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo 'no linter yet'",
    "clean": "rimraf dist || rm -rf dist"
  },
  "dependencies": {
    "@auth4agents/policy": "file:../policy",
    "@auth4agents/credentials": "file:../credentials",
    "@auth4agents/dpop": "file:../dpop",
    "jose": "^5.3.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "rimraf": "^5.0.5",
    "@types/express": "^4.17.21"
  }
}

