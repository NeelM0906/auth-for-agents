{
  "name": "@auth4agents/agent-sdk",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo 'no linter yet'",
    "clean": "rimraf dist || rm -rf dist"
  },
  "dependencies": {
    "@auth4agents/credentials": "file:../credentials",
    "@auth4agents/dpop": "file:../dpop",
    "jose": "^5.3.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "rimraf": "^5.0.5"
  }
}

